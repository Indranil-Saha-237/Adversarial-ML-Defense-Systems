<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Sign Up | Defense System</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;900&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

  <style>
    body {
      background-color: #020202;
      font-family: 'Inter', sans-serif;
      color: #ffffff;
      overflow-x: hidden;
    }

    .top-beam {
      position: fixed;
      top: -10%;
      left: 50%;
      transform: translateX(-50%);
      width: 70%;
      height: 50%;
      background: radial-gradient(circle at 50% 0%, rgba(34, 197, 94, 0.1) 0%, transparent 70%);
      pointer-events: none;
      z-index: 0;
    }

    .center-glow {
      position: fixed;
      top: 50%;
      left: 30%;
      transform: translate(-50%, -50%);
      width: 500px;
      height: 500px;
      background: radial-gradient(circle, rgba(34, 197, 94, 0.08) 0%, transparent 70%);
      filter: blur(80px);
      pointer-events: none;
      z-index: 0;
    }

    .hero-text {
      font-size: 6rem;
      line-height: 0.9;
      letter-spacing: -0.06em;
      font-weight: 900;
    }

    .tech-label {
      font-family: 'JetBrains Mono', monospace;
      text-transform: uppercase;
      font-size: 0.65rem;
      color: #22c55e;
      letter-spacing: 0.1em;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.02);
      backdrop-filter: blur(25px);
      border: 1px solid rgba(255, 255, 255, 0.05);
      border-radius: 32px;
    }

    .bubble-btn {
      background: #0a0a0a;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 9999px;
      padding: 6px 6px 6px 24px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: all 0.3s ease;
      font-size: 0.8rem;
      font-weight: 500;
      width: 100%;
    }

    .bubble-btn:hover {
      border-color: rgba(34, 197, 94, 0.5);
      transform: scale(1.02);
    }

    .bubble-icon-circle {
      width: 32px;
      height: 32px;
      background: #22c55e;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: black;
    }

    input {
      background: rgba(255, 255, 255, 0.03) !important;
      border: 1px solid rgba(255, 255, 255, 0.08) !important;
      border-radius: 14px !important;
      color: white !important;
      font-family: 'JetBrains Mono', monospace;
      transition: 0.3s;
    }

    input:focus {
      border-color: #22c55e !important;
      background: rgba(34, 197, 94, 0.02) !important;
      outline: none;
    }

    .primary-btn {
      background: white;
      color: black;
      border-radius: 9999px;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      transition: 0.3s;
    }

    .primary-btn:hover {
      background: #22c55e;
      color: white;
      box-shadow: 0 0 30px rgba(34, 197, 94, 0.3);
    }
  </style>
</head>
<body class="min-h-screen flex flex-col items-center justify-center p-12">
  <div class="top-beam"></div>
  <div class="center-glow"></div>

  <div class="fixed top-12 left-12 z-20">
    <div class="flex items-center gap-3">
      <svg class="w-8 h-8 text-green-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
      </svg>
      <span class="font-black text-2xl tracking-tighter">ADV<span class="text-green-500">DEF</span></span>
    </div>
  </div>

  <main class="relative z-10 w-full max-w-6xl flex flex-col md:flex-row items-center gap-24">
    <div class="flex-1">
      <div class="tech-label mb-8 flex items-center gap-2">
        <span>[</span> <span class="text-white opacity-80">NEW INVESTIGATOR</span> <span>]</span>
      </div>
      <h1 class="hero-text mb-10">
        Adversarial <br>
        <span class="opacity-40">ML DEFENSE Systems</span>
      </h1>
      <p class="text-gray-400 max-w-lg leading-relaxed text-lg font-light">
        Initialize your operative profile to start deploying adversarial defense mechanisms and safeguarding ML Models.
      </p>

      <div class="mt-16 flex items-center gap-4 text-xs text-gray-500 font-mono tracking-tighter">
        <span class="text-green-500 text-lg">⦿</span>
        <span>SYSTEM READINESS: OPTIMAL</span>
      </div>
    </div>

    <div class="w-full max-w-md">
      <div class="glass-card p-10 shadow-2xl">
        <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
          <div class="bg-red-500/10 border border-red-500/20 text-red-400 px-4 py-3 rounded-2xl mb-6 text-xs text-center">
            <%= error %>
          </div>
        <% } %>

        <form action="/signup" method="POST" class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div class="md:col-span-2 space-y-1">
            <label class="tech-label ml-1 text-[9px] opacity-60">Full Name</label>
            <input type="text" name="name" placeholder="John Doe" required class="w-full px-5 py-3 text-sm">
          </div>
          <div class="md:col-span-2 space-y-1">
            <label class="tech-label ml-1 text-[9px] opacity-60">Operative Email</label>
            <input type="email" name="email" placeholder="you@dotdna.io" required class="w-full px-5 py-3 text-sm">
          </div>
          <div class="space-y-1">
            <label class="tech-label ml-1 text-[9px] opacity-60">Security Cipher</label>
            <input type="password" name="password" placeholder="Min 6 chars" required minlength="6" class="w-full px-5 py-3 text-sm">
          </div>
          <div class="space-y-1">
            <label class="tech-label ml-1 text-[9px] opacity-60">Confirm Cipher</label>
            <input type="password" name="confirmPassword" placeholder="••••••••" required class="w-full px-5 py-3 text-sm">
          </div>

          <button type="submit" class="primary-btn md:col-span-2 w-full py-4 text-xs mt-4">
            Create System Identity
          </button>
        </form>

        <div class="py-6 flex items-center gap-4">
          <div class="flex-1 h-[1px] bg-white/5"></div>
          <span class="tech-label text-[9px] opacity-40">OR</span>
          <div class="flex-1 h-[1px] bg-white/5"></div>
        </div>

        <a href="/auth/google" class="block">
          <div class="bubble-btn">
            <span>Continue with Google</span>
            <div class="bubble-icon-circle">
              <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                <line x1="7" y1="17" x2="17" y2="7"></line>
                <polyline points="7 7 17 7 17 17"></polyline>
              </svg>
            </div>
          </div>
        </a>

        <div class="mt-8 text-center">
          <a href="/login" class="text-[11px] uppercase tracking-widest text-gray-500 hover:text-green-500 transition-colors">
            Already have an identity? <span class="text-green-500">Sign In</span>
          </a>
        </div>
      </div>
    </div>
  </main>

  <footer class="fixed bottom-10 z-20">
    <div class="bg-[#0a0a0a] border border-white/5 backdrop-blur-xl px-8 py-4 rounded-2xl flex items-center gap-10 text-[10px] font-bold tracking-[0.2em] uppercase text-gray-500">
      <span class="hover:text-white cursor-pointer transition-all">Home</span>
      <span class="hover:text-white cursor-pointer transition-all">Services</span>
      <span class="hover:text-white cursor-pointer transition-all">Product</span>
      <span class="hover:text-white cursor-pointer transition-all">About us</span>
    </div>
  </footer>
</body>
</html>