<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Command Center | ADVDEF</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;900&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">

  <style>
    /* 1. SCROLL SETUP */
    html { scroll-behavior: smooth; }
    body {
      background-color: #020202;
      font-family: 'Inter', sans-serif;
      color: #ffffff;
      margin: 0;
      overflow: hidden; /* Scroll handled by snap-container */
    }

    .snap-container {
      height: 100vh;
      overflow-y: scroll;
      snap-type: y mandatory;
      scrollbar-width: none;
    }
    .snap-container::-webkit-scrollbar { display: none; }

    .snap-section {
      height: 100vh;
      width: 100%;
      snap-align: start;
      position: relative;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 10%;
    }

    /* 2. HORIZONTAL SCROLLER */
    .horizontal-hub {
      display: flex;
      overflow-x: scroll;
      snap-type: x mandatory;
      width: 100vw;
      height: 100%;
      scrollbar-width: none;
    }
    .horizontal-hub::-webkit-scrollbar { display: none; }
    .horizontal-slide {
      flex: 0 0 100vw;
      height: 100%;
      snap-align: center;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 0 10%;
    }

    /* 3. VISUALS */
    .top-beam {
      position: fixed;
      top: -10%;
      left: 50%;
      transform: translateX(-50%);
      width: 80%;
      height: 50%;
      background: radial-gradient(circle at 50% 0%, rgba(34, 197, 94, 0.1) 0%, transparent 70%);
      pointer-events: none;
      z-index: 1;
    }

    .hero-text {
      font-size: clamp(4rem, 8vw, 7rem);
      line-height: 0.9;
      letter-spacing: -0.06em;
      font-weight: 900;
    }

    .glass-card {
      background: rgba(255, 255, 255, 0.02);
      backdrop-filter: blur(30px);
      border: 1px solid rgba(255, 255, 255, 0.05);
      border-radius: 40px;
      width: 100%;
      max-width: 900px;
      min-height: 500px;
    }

    .tech-label {
      font-family: 'JetBrains Mono', monospace;
      text-transform: uppercase;
      font-size: 0.65rem;
      color: #22c55e;
      letter-spacing: 0.2em;
    }

    .logout-bubble {
      background: #0a0a0a;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 9999px;
      padding: 4px 4px 4px 20px;
      display: flex;
      align-items: center;
      gap: 16px;
      font-size: 0.75rem;
      font-weight: 600;
      transition: all 0.3s ease;
    }
    
    .logout-bubble:hover {
      border-color: #ef4444;
      background: rgba(239, 68, 68, 0.1);
    }

    .logout-circle {
      width: 32px;
      height: 32px;
      background: white;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      color: black;
      transition: 0.3s;
    }

    .logout-bubble:hover .logout-circle {
      background: #ef4444;
      color: white;
    }

    @keyframes growV { 
      0% { height: 0; opacity: 0; } 
      50% { height: 48px; opacity: 1; } 
      100% { height: 48px; opacity: 0; } 
    }
    .animate-grow-v { animation: growV 2s infinite ease-in-out; }
  </style>
</head>
<body>
  <div class="top-beam"></div>

  <header class="fixed top-12 left-12 right-12 z-50 flex justify-between items-center pointer-events-none">
    <div class="flex items-center gap-3 pointer-events-auto">
      <svg class="w-8 h-8 text-green-500" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
        <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
      </svg>
      <span class="font-black text-2xl tracking-tighter">ADV<span class="text-green-500">DEF</span></span>
    </div>

    <a href="/logout" class="pointer-events-auto">
      <div class="logout-bubble">
        <span>TERMINATE</span>
        <div class="logout-circle">
          <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3">
            <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
            <polyline points="16 17 21 12 16 7"></polyline>
            <line x1="21" y1="12" x2="9" y2="12"></line>
          </svg>
        </div>
      </div>
    </a>
  </header>

  <div class="snap-container">

    <section class="snap-section">
      <div class="max-w-5xl w-full">
        <div class="tech-label mb-8 flex items-center gap-3">
          <div class="w-2 h-2 bg-green-500 rounded-full animate-pulse"></div>
          <span>[ SESSION INITIALIZED ]</span>
        </div>
        <h1 class="hero-text mb-12">
          System<br>
          <span class="opacity-40">Command Centre</span>
        </h1>
        <p class="text-gray-400 text-2xl font-light max-w-2xl leading-relaxed">
          Welcome back, <span class="text-white font-semibold"><%= user.name %></span>. All defense nodes are online. Scroll to access system metadata and operative nodes.
        </p>

        <div class="mt-20 flex flex-col items-center w-fit gap-4 opacity-30">
          <span class="tech-label text-[10px]">Scroll Down</span>
          <div class="w-[1px] h-12 bg-white animate-grow-v"></div>
        </div>
      </div>
    </section>

    <section class="snap-section !p-0">
      <div class="horizontal-hub">


        <div class="horizontal-slide">
          <div class="glass-card p-16 flex flex-col justify-between border-green-500/40 shadow-[0_0_50px_rgba(34,197,94,0.1)]">
            <div>
              <div class="tech-label mb-12">[ 01. ACCOUNT METADATA ]</div>
              <h2 class="text-6xl font-black tracking-tighter mb-16">Profile <span class="opacity-30">Telemetry</span></h2>

              <div class="grid grid-cols-2 gap-y-12 gap-x-20">
                <div>
                  <div class="tech-label text-[10px] opacity-40 mb-2">Operative ID</div>
                  <div class="text-2xl font-medium"><%= user._id.toString().slice(-8).toUpperCase() %></div>
                </div>
                <div>
                  <div class="tech-label text-[10px] opacity-40 mb-2">Auth Level</div>
                  <div class="text-2xl font-medium">Standard Operative</div>
                </div>
                <div>
                  <div class="tech-label text-[10px] opacity-40 mb-2">Registry Date</div>
                  <div class="text-2xl font-medium"><%= new Date(user.createdAt).toLocaleDateString() %></div>
                </div>
                <div>
                  <div class="tech-label text-[10px] opacity-40 mb-2">Status</div>
                  <div class="text-2xl font-medium text-green-500">SYSTEMS ACTIVE</div>
                </div>
              </div>
            </div>

            <div class="flex justify-end gap-4 items-center tech-label text-[10px]">
              <span>Swipe Left for Identity Node</span>
              <div class="w-10 h-[1px] bg-green-500"></div>
            </div>
          </div>
        </div>

        <div class="horizontal-slide">
          <div class="glass-card p-16 flex flex-col justify-between border-green-500/20">
            <div>
              <div class="tech-label mb-12">[ 02. IDENTITY NODE ]</div>
              <div class="flex items-center gap-10 mb-16">
                <div class="w-32 h-32 rounded-full border border-white/10 bg-white/5 flex items-center justify-center text-5xl font-black">
                  <%= user.name.charAt(0).toUpperCase() %>
                </div>
                <div>
                  <h2 class="text-5xl font-black tracking-tighter mb-2"><%= user.name %></h2>
                  <p class="text-gray-500 font-mono"><%= user.email %></p>
                </div>
              </div>

              <div class="bg-white/5 rounded-3xl p-8 border border-white/5">
                <div class="tech-label text-[10px] mb-4">Encryption Method</div>
                <div class="text-xl font-mono opacity-80">
                  <%= user.googleId ? 'GOOGLE_OAUTH_v2_ENCRYPTED' : 'AES_256_SHA_HASHED' %>
                </div>
              </div>
            </div>

            <div class="flex justify-end gap-4 items-center tech-label text-[10px]">
              <span>Swipe Left for Audit Logs</span>
              <div class="w-10 h-[1px] bg-green-500"></div>
            </div>
          </div>
        </div>

        <div class="horizontal-slide">
          <div class="glass-card p-16">
            <div class="tech-label mb-12">[ 03. INTEGRITY ACTIONS ]</div>
            <h2 class="text-6xl font-black tracking-tighter mb-16">Audit <span class="opacity-30">& Logs</span></h2>

            <div class="space-y-4">
              <button class="w-full bg-white text-black py-6 rounded-2xl font-bold uppercase tracking-widest hover:bg-green-500 hover:text-white transition-all">
                Export System Audit
              </button>
              <button class="w-full bg-transparent border border-white/10 py-6 rounded-2xl font-bold uppercase tracking-widest hover:border-green-500/50 transition-all">
                Update Access Cipher
              </button>
              <button class="w-full bg-transparent border border-white/10 py-6 rounded-2xl font-bold uppercase tracking-widest hover:border-red-500/50 text-red-500 transition-all">
                Revoke All Sessions
              </button>
            </div>
          </div>
        </div>

      </div>
    </section>
  </div>

  <footer class="fixed bottom-10 left-1/2 -translate-x-1/2 z-50">
    <div class="bg-[#0a0a0a]/80 border border-white/5 backdrop-blur-xl px-10 py-5 rounded-full flex items-center gap-12 text-[10px] font-bold tracking-[0.2em] uppercase text-gray-400">
      <div class="flex items-center gap-3 text-white">
        <div class="w-1.5 h-1.5 bg-green-500 rounded-full shadow-[0_0_8px_#22c55e]"></div>
        Overview
      </div>
      <span class="hover:text-white cursor-pointer transition-colors">Nodes</span>
      <span class="hover:text-white cursor-pointer transition-colors">Firewall</span>
      <span class="hover:text-white cursor-pointer transition-colors">Integrity</span>
    </div>
  </footer>

</body>
</html>